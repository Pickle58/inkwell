# Copied/created by `npx convex dev`
CONVEX_DEPLOYMENT=
NEXT_PUBLIC_CONVEX_URL=
NEXT_PUBLIC_CONVEX_SITE_URL=

# Optional Next.js fallback if Convex Polar env isn't configured yet.
# Must be a sandbox URL (sandbox-api.polar.sh / sandbox.polar.sh), never buy.polar.sh.
NEXT_PUBLIC_POLAR_CHECKOUT_LINK=https://sandbox-api.polar.sh/v1/checkout-links/polar_cl_NUp71gmInplx2XKIXQItYqLuWh41XFwxgor4M0JIM5P/redirect

# Set on the Convex deployment (not Next.js):
# npx convex env set OPENAI_API_KEY sk-...
# npx convex env set SITE_URL http://localhost:3000
#
# Polar sandbox (https://sandbox.polar.sh):
# npx convex env set POLAR_SERVER sandbox
# npx convex env set POLAR_ACCESS_TOKEN polar_oat_...
# npx convex env set POLAR_PRODUCT_ID a9744e34-6d5a-42ea-8d43-5313a3b0a518
# npx convex env set POLAR_WEBHOOK_SECRET whsec_...
#
# Polar dashboard checklist:
# 1. Product: Inkwell Monthly ($19/mo, 5-day trial)
# 2. Settings → Preferences → Embedding: add localhost:3000
# 3. Webhook: https://<deployment>.convex.site/polar/webhook
#    Events: subscription.created, subscription.updated, subscription.active,
#            subscription.canceled, subscription.uncanceled, subscription.revoked,
#            subscription.past_due
